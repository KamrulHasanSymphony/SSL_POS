@model ShampanPOS.Models.SupplierVM
@{
    ViewBag.Title = "Supplier Report";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<style>
    body {
        font-family: Arial;
        background: #f2f2f2;
    }

    .report-box {
        background: white;
        padding: 20px;
        border: 1px solid #ccc;
        max-width: 900px;
        margin: 20px auto;
        border-radius: 6px;
    }

    .sslPrint {
        background: #17a2b8;
        color: white;
        padding: 8px 20px;
        border-radius: 4px;
        border: none;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
        font-size: 13px;
    }

        table th, table td {
            border: 1px solid #777;
            padding: 6px;
        }

        table th {
            background: #e9e9e9;
            font-weight: bold;
        }

    @@media print {
        .sslPrint {
            display: none
        }

        .report-box {
            border: none !important;
            box-shadow: none !important;
        }
    }
</style>

<button onclick="window.print()" class="sslPrint" style="float:right;margin:20px;">
    <i class="k-icon k-i-print"></i> Print
</button>

<div class="report-box">

    <!-- HEADER -->
    <table style="border:none;">
        <tr>
            <td style="width:140px;">
                <img src="~/Content/AdminLte/dist/img/logo.png"
                     style="width:130px;height:70px;object-fit:contain;" />
            </td>
            @*<td style="text-align:right;border:none;">
            <h3 style="margin:0;">@Model.CompanyName</h3>
            <div>@Model.Address1</div>
            <div>📞 @Model.TelephoneNo</div>
            <div>✉️ @Model.Email</div>
        </td>*@
        </tr>
    </table>

    <h2 style="text-align:center;border-bottom:1px solid #000;">
        SUPPLIER REPORT
    </h2>

    <!-- SUPPLIER BASIC INFO -->
    <h3>Supplier Information</h3>
    <table>
        <tr>
            <th style="width:160px;">Code</th>
            <td>@Model.Code</td>
            <th style="width:160px;">Name</th>
            <td>@Model.Name</td>
        </tr>
        <tr>
            <th>Bangla Name</th>
            <td>@Model.BanglaName</td>
            <th>Supplier Group</th>
            <td>@Model.SupplierGroupId</td>
        </tr>
    </table>

    <!-- CONTACT INFO -->
    <h3>Contact Information</h3>
    <table>
        <tr>
            <th>Address</th>
            <td>@Model.Address</td>
            <th>City</th>
            <td>@Model.City</td>
        </tr>
        <tr>
            <th>Telephone</th>
            <td>@Model.TelephoneNo</td>
            <th>Email</th>
            <td>@Model.Email</td>
        </tr>
        <tr>
            <th>Contact Person</th>
            <td colspan="3">@Model.ContactPerson</td>
        </tr>
    </table>

    <!-- STATUS -->
    <h3>Status</h3>
    <table>
        <tr>
            <th>Active</th>
            <td>@(Model.IsActive ? "Yes" : "No")</td>
            <th>Archived</th>
            <td>@(Model.IsArchive ? "Yes" : "No")</td>
        </tr>
    </table>

</div>



